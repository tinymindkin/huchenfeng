### 将train_dialogues的process_data
---
轮询 ：for train_item in train_dialogues 表
    if train_item.status = 1:
        if train_item.processed_data is not None:
            answser_list = json.loads(train_item.processed_data)
            for answer in answser_list:
                generate_question_result = generate_question(answer)[0]
                if generate_question_result is None:
                    continue
                qustion_str, model_version = generate_question_result
                insert question_answer_table (answer_id,answer,question,model_version,status) values (train_item.id,answer,qustion_str,model_version,1)


 
### CONTEXT
CREATE TABLE IF NOT EXISTS question_answer_table (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    answer_id INTEGER NOT NULL,
    question TEXT,
    answer TEXT NOT NULL,
    status INTEGER DEFAULT 0,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (answer_id) REFERENCES train_dialogues(id)
)
CREATE TABLE train_dialogues (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    huchenfeng VARCHAR NOT NULL,
    processed_data TEXT,
    model_version TEXT(100),
    status INTEGER DEFAULT 0,
    source TEXT(100),
    created_at TIMESTAMP NOT NULL,
    processed_at TIMESTAMP
)